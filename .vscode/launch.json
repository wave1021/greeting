{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "node",
            "preLaunchTask": "build",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}
